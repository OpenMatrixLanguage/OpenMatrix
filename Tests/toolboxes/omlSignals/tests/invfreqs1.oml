addtoolbox omlsignals
wc = (2*pi)*200;
order = 4;
[num,den] = butter(order, wc, 's')
w = [1:1:14] * 0.1 * wc;
h = freqs(num,den,w);
[b,a] = invfreqs(h,w,order,order);
mask = 2^14;
b = round(mask * b) / mask + 0	% remove round off
a
