addpath(strcat(getenv('OMLBVT') ,'/RegressionTests/functions'));
addtoolbox omlsignals
wc = (2*pi)*200;
order = 4;
[num,den] = butter(order, wc, 's')
w = [1:1:14] * 0.1 * wc;
h = freqs(num,den,w);
[b,a] = invfreqs(h,w,order,order);
b = lowbitcleanup(b, 0)
a

