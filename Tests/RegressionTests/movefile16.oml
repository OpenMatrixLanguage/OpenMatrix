% movefile-note movefile with spaces with force overwrite

returndir = pwd;
cd FileManipulation

cd movefile

disp('move file with spaces with force overwrite')
command = 'movefile(''test1.txt'', ''test 1.txt'', ''f'')'
eval(command)

dir

disp('move second file with spaces with force overwrite')
command = 'r1 = movefile(''test2.txt'', ''test 2.txt'', ''f'')'
eval(command)

dir

disp('move wildcard files with spaces to dir1 with force overwrite')
command = 'r1 = movefile(''test*.txt'', ''dir1'', ''f'')'
eval(command)

dir

command = 'dir dir1'
eval(command)

command = 'dir dir2'
eval(command)

disp('move wildcard files with spaces from dir1 to dir 2 with force overwrite')
command = 'r1 = movefile(''dir1/test*.txt'', ''dir2'', ''f'')'
eval(command)

dir

command = 'dir dir1'
eval(command)

command = 'dir dir2'
eval(command)

disp('move wildcard files with spaces from dir2 to current dir with force overwrite')
command = 'r1 = movefile(''dir2/test*.txt'', ''.'', ''f'')'
eval(command)

dir

command = 'dir dir1'
eval(command)

command = 'dir dir2'
eval(command)

disp('move file with spaces to remove spaces with force overwrite')
command = 'movefile(''test 1.txt'', ''test1.txt'', ''f'')'
eval(command)

dir

disp('move second file with spaces to remove spaces with force overwrite')
command = 'r1 = movefile(''test 2.txt'', ''test2.txt'', ''f'')'
eval(command)

dir

cd(returndir)
