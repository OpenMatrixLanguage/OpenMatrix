% movefile-note movefile cleanup

returndir = pwd;
cd FileManipulation

cd movefile
dir

disp('delete files')
command = 'delete test1.txt'
eval(command)
command = 'delete test2.txt'
eval(command)

disp('remove directories')
command = 'rmdir dir1'
eval(command)
command = 'rmdir dir2'
eval(command)

dir

cd ..

command = 'rmdir movefile'
eval(command)

cd(returndir)
