% mult #1 (sparse * full)
m11 = sparse(zeros(4,3)) * reshape([1:6],[3,2])
m12 = sparse(reshape([1:6],[2,3])) * zeros(3,4)
m13 = sparse(zeros(3,3)) * zeros(3,0)
m14 = sparse(zeros(3,0)) * zeros(0,3)
m15 = sparse(zeros(0,3)) * zeros(3,3)

% mult #2 (full * sparse)
m21 = reshape([1:6],[2,3]) * sparse(zeros(3,4))
m22 = zeros(4,3) * sparse(reshape([1:6],[3,2]))
m23 = zeros(0,3) * sparse(zeros(3,3))
m24 = zeros(3,0) * sparse(zeros(0,3))
m25 = zeros(3,3) * sparse(zeros(3,0))

% mult #3 (sparse * sparse)
m31 = sparse(zeros(4,3)) * sparse(reshape([1:6],[3,2]))
m32 = sparse(reshape([1:6],[2,3])) * sparse(zeros(3,4))
m33 = sparse(zeros(3,3)) * sparse(zeros(3,0))
size(m33)
m34 = sparse(zeros(3,0)) * sparse(zeros(0,3))
m35 = sparse(zeros(0,3)) * sparse(zeros(3,3))
size(m35)
