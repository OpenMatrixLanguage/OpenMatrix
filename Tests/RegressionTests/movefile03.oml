% movefile-note move a single file to a directory.

returndir = pwd;
cd FileManipulation

cd movefile
dir

% move file command does not work for this
command = 'movefile(''test1.txt'', ''dir1'')'
eval(command)

%command = 'system(''move test1.txt dir1'')'
%eval(command)

dir

command = 'dir dir1'
eval(command)

command = 'movefile(''dir1/test1.txt'', ''test1.txt'')'
eval(command)

dir 

command = 'dir dir1'
eval(command)

cd(returndir)

