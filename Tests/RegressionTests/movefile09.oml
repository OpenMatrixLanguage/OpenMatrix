% movefile-note move directory to a sub-directory. then move wildcard contents back to the current directory 

returndir = pwd;
cd FileManipulation

cd movefile
dir

command = 'movefile(''dir1'', ''dir2'')'
eval(command)

dir

command = 'dir dir1'
eval(command)

command = 'dir dir2'
eval(command)

command = 'movefile(''dir2/*'', ''.'')'
eval(command)

dir

command = 'dir dir1'
eval(command)

command = 'dir dir2'
eval(command)

cd(returndir)
