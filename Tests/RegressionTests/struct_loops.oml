Vertices(1).coord=[0 0]; 
Vertices(1).value=0;
Vertices(2).coord=[1.2 0]; 
Vertices(2).value=-3.36;
Vertices(3).coord=[0 0.8]; 
Vertices(3).value=-0.16;

for i = 1:2
	temp = Vertices(i+1);
	Vertices(i+1) = Vertices(i);
	Vertices(i) = temp;
end

for i = 1:3
	disp(Vertices(i).coord);
end
