fname = 'FileManipulation/FileToRead.txt'
[fid1, error] = fopen(fname)
[fid2, error] = fopen(fname)
[filepath, filemode] = fopen(fid1)
[text, len] = fgets(fid1);
text
if (ispc && len == 11) || (isunix && len == 10)
	disp('len returns expected value: 11 for Windows, 10 for Linux')
else
	printf('len unexpected return value: %d\n', len)
end

[text, len] = fgets(fid2);
text
if (ispc && len == 11) || (isunix && len == 10)
	disp('len returns expected value: 11 for Windows, 10 for Linux')
else
	printf('len unexpected return value: %d\n', len)
end

fclose(fid1)
fclose(fid2)
